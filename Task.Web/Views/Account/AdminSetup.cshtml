@model Task.Web.Models.AccountModels.RegisterModel
@{
    ViewBag.Title = "Admin";
}
<div>
    You must configure the administrator account before the application start
</div>
<div>
@using (Html.BeginForm("AdminSetup", "Account"))
{
    @Html.EditorForModel()
    <div class="text-center">
        <input type="submit" class="button" value="Register" />
        <input type="button" id="btn_cancel" class="button" value="Cancel" onclick="goToPage('@Url.Action("Index", "Home")')" />
    </div>
}
</div>